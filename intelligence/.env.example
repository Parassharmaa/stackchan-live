# Eve uses the local Codex login and the Responses transport. The Luna tier is
# the strict-latency baseline; run `codex login` before starting Eve.
STACKCHAN_EVE_MODEL=gpt-5.6-luna
STACKCHAN_CORE_URL=http://127.0.0.1:8765
