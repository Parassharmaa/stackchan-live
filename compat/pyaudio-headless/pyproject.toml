[project]
name = "PyAudio"
version = "0.2.14"
description = "Headless compatibility package for Stack-chan's PCM-fed MaAI sidecar"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pyaudio"]
